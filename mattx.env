#!/bin/bash
# adds the scripts directory to the PATH variable so that the scripts can be run from anywhere

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
PATH=$PATH:$SCRIPT_DIR/scripts
export PATH
